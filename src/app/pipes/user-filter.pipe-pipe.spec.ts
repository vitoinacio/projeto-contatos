import { UserFilterPipePipe } from './user-filter.pipe-pipe';

describe('UserFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
